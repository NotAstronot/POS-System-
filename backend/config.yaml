server:
  port: "8080"
  admin_username: admin
  admin_password: admin123

database:
  dialect: postgres
  host: postgres
  port: "5432"
  user: postgres
  password: postgres
  dbname: pos_system
  sslmode: disable
  max_open_conns: 25
  max_idle_conns: 10
  conn_max_lifetime: 300

jwt:
  secret_key: "change-this-to-strong-secret-in-production"
